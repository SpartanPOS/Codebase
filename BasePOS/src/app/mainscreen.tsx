'use client'; // If this component needs to use hooks

import * as React from 'react';

const Mainscreen: React.FC = () => {
  return (
    <div style={{width: 400}}>
      Lorem
    </div>
  );
};

export default Mainscreen;
